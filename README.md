Задание для Пет-Проекта: "Weather Forecast Quality Monitor"

1. Цель Проекта
Разработать автоматизированную систему (скрипт) для сбора, хранения и ежедневной оценки качества прогнозов погоды от внешнего источника относительно фактических погодных наблюдений. Результаты оценки должны регулярно отправляться в мессенджер для оперативного мониторинга.
🛠️ 2. Стек Технологий (Рекомендация)
Категория	Технология	Примечание
Язык	Python	Идеален для работы с API, данными и интеграции с ботами.
База Данных	SQLite / PostgreSQL	SQLite для простого старта, PostgreSQL для масштабируемости.
Источники Данных	2 разных API	Один для Прогноза (например, OpenWeatherMap One Call API) и второй для Фактических Данных (например, API метеостанций или того же OpenWeatherMap Current Weather, но с задержкой в 24 часа).
Мессенджер	Telegram	Для отправки уведомлений (нужен Telegram Bot Token).
Автоматизация	cron / APScheduler	Для ежедневного запуска скрипта.

📝 3. Детальный План (Пайплайн)
Проект должен быть реализован в виде единого Python-скрипта или набора модулей, запускаемых ежедневно.
Этап 1: Сбор и Хранение Данных (The Collector)
Задача 1.1: Получение Прогноза (F)
    1. Определение: Выберите Целевой Город и Горизонт Прогноза (например, прогноз на +24 часа).
    2. API Call: Запросите прогноз погоды (температура, осадки) на завтра в текущий день.
    3. Хранение: Занесите полученный прогноз в таблицу forecasts.
Задача 1.2: Получение Фактических Данных (O) — "Ground Truth"
    1. API Call: Запросите фактические данные о погоде в Целевом Городе, но для вчерашней даты.
    2. Хранение: Занесите полученное наблюдение в таблицу observations.
Задача 1.3: База Данных (Data Model)
Разработайте схему с двумя основными таблицами:
Таблица: forecasts	Таблица: observations
id (PK)	id (PK)
location (TEXT)	location (TEXT)
forecast_datetime (DATETIME)	actual_datetime (DATETIME)
temp_max_f (REAL)	temp_max_o (REAL)
rain_prob_f (REAL)	rain_event_o (BOOLEAN)
source (TEXT)	source (TEXT)
created_at (DATETIME)	created_at (DATETIME)
Этап 2: Расчет Качества (The Evaluator)
Задача 2.1: Сопоставление Данных
    1. Выполните SQL-запрос для объединения прогнозов и наблюдений, где forecast_datetime из таблицы forecasts соответствует actual_datetime из таблицы observations.
Задача 2.2: Расчет Метрик
Рассчитайте качество прогноза по ключевым переменным:
    1. Для Температуры (Непрерывная переменная):
        ◦ Средняя Абсолютная Ошибка (MAE): $|F_{temp\_max} - O_{temp\_max}|$
        ◦ Смещение (Bias): $F_{temp\_max} - O_{temp\_max}$ (показывает, переоценили или недооценили)
    2. Для Осадков (Бинарная переменная):
        ◦ Сконвертируйте rain_prob_f в бинарный прогноз (например, "Да" при вероятности $\ge 50\%$).
        ◦ Коэффициент Сходимости (Accuracy).
        ◦ Вероятность Обнаружения (POD) / Чувствительность.
        ◦ Индекс Угрозы (CSI).
Задача 2.3: Хранение Результатов
Создайте отдельную таблицу evaluation_results для хранения ежедневных метрик, включая:
    • date, MAE_temp, Bias_temp, Accuracy_rain, CSI_rain, и т.д.
    • commentary (текстовое поле для автоматической интерпретации).
Этап 3: Отчетность и Автоматизация (The Reporter)
Задача 3.1: Логирование в Мессенджер
Реализуйте функцию send_telegram_message(text), которая будет отправлять сообщения в ваш канал/чат:
    1. Логи Выполнения: Отправляйте сообщение о начале и успешном завершении каждого из основных этапов (1.1, 1.2, 2.2). Если API вернул ошибку, отправьте сообщение об ошибке.
    2. Результаты Расчетов: Отправляйте итоговое сообщение с рассчитанными метриками.
Задача 3.2: Интерпретация Результатов (Комментарии)
Сгенерируйте краткий комментарий, основываясь на результатах:
    • Пример 1 (MAE): "Средняя абсолютная ошибка прогноза максимальной температуры за вчера составила 1.5°C. Прогноз был систематически завышен (Bias: +1.2°C)."
    • Пример 2 (CSI): "Индекс Угрозы для прогноза дождя составил 0.8. Это означает, что 8 из 10 фактических дождей были предсказаны, с учетом неверных тревог."
Задача 3.3: Автоматический Запуск
Настройте планировщик (cron на Linux/macOS или Task Scheduler на Windows, или используйте Python-библиотеку) для ежедневного запуска скрипта (например, в 10:00 утра).

✅ 4. Критерии Успеха (MVP)
Проект считается минимально жизнеспособным продуктом (MVP), если:
    1. Скрипт автоматически запускается каждый день.
    2. Данные прогноза (F) и наблюдения (O) успешно извлекаются, сопоставляются и сохраняются в БД.
    3. Рассчитаны хотя бы две ключевые метрики: MAE для температуры и CSI для осадков.
    4. В Telegram каждый день приходит сообщение, содержащее:
        ◦ Подтверждение успешного завершения всех этапов.
        ◦ Фактическое значение MAE (например, 1.5°C).
        ◦ Сгенерированный комментарий с интерпретацией результата.


Резюме Технического Стека
Компонент	Технология	Роль в Проекте
Язык Скрипта	Python	Основной язык для работы с API, обработки данных и расчёта метрик.
Автоматизация	GitHub Actions	Бесплатный облачный планировщик задач (cron), который будет ежедневно запускать Python-скрипт.
База Данных	Supabase	Управляемая PostgreSQL база данных для надёжного хранения прогнозов, наблюдений и результатов оценки.
Нотификации	Telegram Bot	Средство оповещения о статусе выполнения скрипта и отправки ежедневных отчётов о качестве прогноза.
